function func(){
    // 函数

    // 参数
    // 传参不一定要与函数匹配。没有传的参数自动被赋予Undefined
    // 原因是ECMAScript中的参数在内部是用一个数组来表示的，参数接收到的始终都是这个数组，而不关心数组中包含哪个参数。
    // arguments 包含参数数组 可以与命名参数一起使用

    // ECMAScript中的函数没有重载。
}