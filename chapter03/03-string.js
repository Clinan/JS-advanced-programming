// string 
function stringFunction() {
    // 字符串的特点
    /*
        ECMAScript中的字符串是不可变的，也就是说，字符串一旦创建，他们的值就不能改变。
        要改变某个变量保存的字符串，首先要销毁原来的字符串，然后再用另一个包含新值的字符串填充该变量。
        这个特性跟Java中的String是一致的。
    */

    // 转换为字符串 toString() String()
    // null      => 'null'
    // undefined => 'undefined'

}